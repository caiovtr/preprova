import { StyleSheet } from "react-native";

export const Painel = StyleSheet.create({
    container: {
        alignItems: "center",
        justifyContent: "center",
    },
    image: {
        width: 250,
        height: 200
    }
})